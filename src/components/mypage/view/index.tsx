"use client";
import { FC } from "react";
import { useForm, SubmitHandler } from "react-hook-form";
interface Props {
  write: boolean;
}

const ViewComponent: FC<Props> = ({ write }) => {
  return null;
};

export default ViewComponent;
